# AgentSkin integration guide

Use `compress` as the default entry point for existing context. Use `fetch_optimized_data` when AgentSkin should retrieve a public resource, and `reduce` when command output and the original command are already known.

Use `skin_reasoning` only when wording changes are explicitly acceptable.
