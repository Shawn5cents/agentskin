# AgentSkin FAQ

## What is AgentSkin?
Context middleware that reduces low-value API, JSON, and terminal output before it enters model context.

## Does it guarantee a fixed savings percentage?
No. Savings vary by workload. Fidelity is the primary release criterion.

## What powers CLI reduction?
Tokenjuice, consumed as an npm dependency under its MIT license.
